<%= simple_form_for(@booking) do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.association :cottage %>
  <%= f.association :user %>
  <%= f.submit "Submit", class: "validate-button form-button" %>
<% end %>
